# Gateway Service - Variables de entorno requeridas
# Copia este archivo como .env y llena los valores reales

SERVER_PORT=8080

# Seguridad JWT (debe ser identico al secreto en Auth service)
JWT_SECRET=your_jwt_secret_key_here
