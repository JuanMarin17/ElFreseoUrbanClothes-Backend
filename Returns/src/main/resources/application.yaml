server:
  port: 8092
  servlet:
    context-path: /api/v1

spring:
  application:
    name: returns

  datasource:
    url: jdbc:postgresql://localhost:5432/returns
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

order:
  service:
    url: http://localhost:8087/api/v1
