# ─────────────────────────────────────────────────────────────────────────────
# Vexio — Docker Compose
#
# ANTES DE USAR:
#   1. Ejecuta build-all.ps1 para compilar todos los JARs
#   2. Crea un archivo .env en esta carpeta con tus variables secretas:
#        NAME_CLOUD=tu_cloudinary_name
#        KEY_CLOUD=tu_cloudinary_key
#        SECRET_CLOUD=tu_cloudinary_secret
#   3. Levanta todo con: docker-compose up -d
#   4. Para ver los logs: docker-compose logs -f
#   5. Para detener: docker-compose down
# ─────────────────────────────────────────────────────────────────────────────

networks:
  vexio-net:
    driver: bridge   # Red interna compartida por todos los contenedores

volumes:
  postgres-data:     # Volumen persistente: los datos de la BD sobreviven reinicios

services:

  # ── POSTGRESQL ──────────────────────────────────────────────────────────────
  # Un solo contenedor de PostgreSQL aloja las 15 bases de datos.
  # El archivo init.sql se ejecuta automáticamente la primera vez.
  postgres:
    image: postgres:16-alpine
    container_name: vexio-postgres
    command: postgres -c max_connections=300   # 15 servicios x 5 pool = 75, con margen
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./docker/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    ports:
      - "127.0.0.1:5432:5432"   # Solo accesible desde localhost del VPS
    networks:
      - vexio-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 10     # Los servicios esperan a que Postgres esté listo antes de iniciar

  # ── SERVICIOS SIN DEPENDENCIAS ENTRE MICROSERVICIOS ─────────────────────────
  # Estos servicios solo dependen de la base de datos.

  auth-service:
    build:
      context: ./Auth
      dockerfile: ../Dockerfile   # Dockerfile genérico en la raíz del proyecto
    container_name: auth-service
    ports:
      - "127.0.0.1:8082:8082"
    environment:
      SERVER_PORT: 8082
      # SPRING_DATASOURCE_URL sobreescribe spring.datasource.url del application.yaml
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/auth
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}
      DB_URL: jdbc:postgresql://postgres:5432/auth   # por si el YAML usa ${DB_URL}
      DB_USER: postgres
      DB_PASSWORD: ${DB_PASSWORD}
      SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE: 5
      USERS_SERVICE_URL: http://users-service:8083/api/v1
      JWT_SECRET: ${JWT_SECRET}
      MAIL_USERNAME: ${MAIL_USERNAME}
      MAIL_PASSWORD: ${MAIL_PASSWORD}
    depends_on:
      postgres:
        condition: service_healthy   # Espera que Postgres pase el healthcheck
    networks:
      - vexio-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost 8082 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 90s

  users-service:
    build:
      context: ./Users
      dockerfile: ../Dockerfile
    container_name: users-service
    ports:
      - "127.0.0.1:8083:8083"
    environment:
      SERVER_PORT: 8083
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/users
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}
      DB_URL: jdbc:postgresql://postgres:5432/users
      DB_USER: postgres
      DB_PASSWORD: ${DB_PASSWORD}
      SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE: 5
      AUTH_SERVICE_URL: http://auth-service:8082/api/v1
      USERS_SERVICE_URL: http://auth-service:8082/api/v1
      STORE_SERVICE_URL: http://store-service:8081/api/v1
      NAME_CLOUD: ${NAME_CLOUD}
      KEY_CLOUD: ${KEY_CLOUD}
      SECRET_CLOUD: ${SECRET_CLOUD}
      MEDIA_SERVICE_URL: http://media-service:8096
      SPRING_SERVLET_MULTIPART_MAX_FILE_SIZE: 10MB
      SPRING_SERVLET_MULTIPART_MAX_REQUEST_SIZE: 10MB
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - vexio-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8083/api/v1/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 90s

  store-service:
    build:
      context: ./Store
      dockerfile: ../Dockerfile
    container_name: store-service
    ports:
      - "127.0.0.1:8081:8081"
    environment:
      SERVER_PORT: 8081
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/store
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}
      DB_URL: jdbc:postgresql://postgres:5432/store
      DB_USER: postgres
      DB_PASSWORD: ${DB_PASSWORD}
      SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE: 5
      USERS_SERVICE_URL: http://users-service:8083/api/v1
      JWT_SECRET: ${JWT_SECRET}
      NAME_CLOUD: ${NAME_CLOUD}
      KEY_CLOUD: ${KEY_CLOUD}
      SECRET_CLOUD: ${SECRET_CLOUD}
      MEDIA_SERVICE_URL: http://media-service:8096
      SPRING_SERVLET_MULTIPART_MAX_FILE_SIZE: 10MB
      SPRING_SERVLET_MULTIPART_MAX_REQUEST_SIZE: 10MB
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - vexio-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost 8081 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 90s

  product-service:
    build:
      context: ./Product
      dockerfile: ../Dockerfile
    container_name: product-service
    ports:
      - "127.0.0.1:8084:8084"
    environment:
      SERVER_PORT: 8084
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/product
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}
      DB_URL: jdbc:postgresql://postgres:5432/product
      DB_USER: postgres
      DB_PASSWORD: ${DB_PASSWORD}
      SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE: 5
      STORE_SERVICE_URL: http://store-service:8081/api/v1
      NAME_CLOUD: ${NAME_CLOUD}
      KEY_CLOUD: ${KEY_CLOUD}
      SECRET_CLOUD: ${SECRET_CLOUD}
      MEDIA_SERVICE_URL: http://media-service:8096
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - vexio-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8084/api/v1/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 90s

  cart-service:
    build:
      context: ./Cart
      dockerfile: ../Dockerfile
    container_name: cart-service
    ports:
      - "127.0.0.1:8086:8086"
    environment:
      SERVER_PORT: 8086
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/cart
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}
      DB_URL: jdbc:postgresql://postgres:5432/cart
      DB_USER: postgres
      DB_PASSWORD: ${DB_PASSWORD}
      SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE: 5
      JWT_SECRET: ${JWT_SECRET}
      STORE_SERVICE_URL: http://store-service:8081/api/v1
      PRODUCT_SERVICE_URL: http://product-service:8084/api/v1
    depends_on:
      postgres:
        condition: service_healthy
      store-service:
        condition: service_healthy
      product-service:
        condition: service_healthy
    networks:
      - vexio-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost 8086 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 90s

  supplier-service:
    build:
      context: ./Supplier
      dockerfile: ../Dockerfile
    container_name: supplier-service
    ports:
      - "127.0.0.1:8085:8085"
    environment:
      SERVER_PORT: 8085
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/supplier
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}
      DB_URL: jdbc:postgresql://postgres:5432/supplier
      DB_USER: postgres
      DB_PASSWORD: ${DB_PASSWORD}
      SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE: 5
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - vexio-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8085/api/v1/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 90s

  support-service:
    build:
      context: ./Support
      dockerfile: ../Dockerfile
    container_name: support-service
    ports:
      - "127.0.0.1:8088:8088"
    environment:
      SERVER_PORT: 8088
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/support
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}
      DB_URL: jdbc:postgresql://postgres:5432/support
      DB_USER: postgres
      DB_PASSWORD: ${DB_PASSWORD}
      SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE: 5
      MAIL_USERNAME: ${MAIL_USERNAME}
      MAIL_PASSWORD: ${MAIL_PASSWORD}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - vexio-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8088/api/v1/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 90s

  promotion-service:
    build:
      context: ./Promotion
      dockerfile: ../Dockerfile
    container_name: promotion-service
    ports:
      - "127.0.0.1:8091:8091"
    environment:
      SERVER_PORT: 8091
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/promotion
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}
      DB_URL: jdbc:postgresql://postgres:5432/promotion
      DB_USER: postgres
      DB_PASSWORD: ${DB_PASSWORD}
      SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE: 5
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - vexio-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8091/api/v1/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 90s

  loyal-customer-service:
    build:
      context: ./LoyalCustomer
      dockerfile: ../Dockerfile
    container_name: loyal-customer-service
    ports:
      - "127.0.0.1:8093:8093"
    environment:
      SERVER_PORT: 8093
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/loyalty
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}
      DB_URL: jdbc:postgresql://postgres:5432/loyalty
      DB_USER: postgres
      DB_PASSWORD: ${DB_PASSWORD}
      SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE: 5
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - vexio-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8093/api/v1/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 90s

  preferences-service:
    build:
      context: ./Preferences
      dockerfile: ../Dockerfile
    container_name: preferences-service
    ports:
      - "127.0.0.1:8094:8094"
    environment:
      SERVER_PORT: 8094
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/preferences
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}
      DB_URL: jdbc:postgresql://postgres:5432/preferences
      DB_USER: postgres
      DB_PASSWORD: ${DB_PASSWORD}
      SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE: 5
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - vexio-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8094/api/v1/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 90s

  reviews-service:
    build:
      context: ./Reviews
      dockerfile: ../Dockerfile
    container_name: reviews-service
    ports:
      - "127.0.0.1:8095:8095"
    environment:
      SERVER_PORT: 8095
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/reviews
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}
      DB_URL: jdbc:postgresql://postgres:5432/reviews
      DB_USER: postgres
      DB_PASSWORD: ${DB_PASSWORD}
      SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE: 5
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - vexio-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8095/api/v1/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 90s

  cms-service:
    build:
      context: ./Cms
      dockerfile: ../Dockerfile
    container_name: cms-service
    ports:
      - "127.0.0.1:8101:8101"
    environment:
      SERVER_PORT: 8101
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/cms
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}
      DB_URL: jdbc:postgresql://postgres:5432/cms
      DB_USER: postgres
      DB_PASSWORD: ${DB_PASSWORD}
      SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE: 5
      GEMINI_API_KEY: ${GEMINI_API_KEY}
      PREFERENCES_SERVICE_URL: http://preferences-service:8094
      PRODUCT_SERVICE_URL: http://product-service:8084
    depends_on:
      postgres:
        condition: service_healthy
      preferences-service:
        condition: service_healthy
      product-service:
        condition: service_healthy
    networks:
      - vexio-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost 8101 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 90s

  reports-service:
    build:
      context: ./Reports
      dockerfile: ../Dockerfile
    container_name: reports-service
    ports:
      - "127.0.0.1:8100:8100"
    environment:
      SERVER_PORT: 8100
      PRODUCT_SERVICE_URL: http://product-service:8084/api/v1
      ORDER_SERVICE_URL: http://order-payment-service:8087/api/v1
    depends_on:
      product-service:
        condition: service_healthy
      order-payment-service:
        condition: service_healthy
    networks:
      - vexio-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8100/api/v1/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 90s

  media-service:
    build:
      context: ./media-service
    container_name: media-service
    ports:
      - "127.0.0.1:8096:8096"
    environment:
      PORT: 8096
      NAME_CLOUD: ${NAME_CLOUD}
      KEY_CLOUD: ${KEY_CLOUD}
      SECRET_CLOUD: ${SECRET_CLOUD}
    networks:
      - vexio-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8096/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 20s

  ia-user-service:
    build:
      context: ./IaUser
    container_name: ia-user-service
    ports:
      - "127.0.0.1:8099:8099"
    environment:
      DATABASE_URL: postgresql://postgres:${DB_PASSWORD}@postgres:5432/ia_user
      GEMINI_API_KEY: ${GEMINI_API_KEY}
      GROQ_API_KEY: ${GROQ_API_KEY}
      PRODUCT_SERVICE_URL: http://product-service:8084
      CART_SERVICE_URL: http://cart-service:8086
      SUPPORT_SERVICE_URL: http://support-service:8088
      STORE_SERVICE_URL: http://store-service:8081
      PROMOTIONS_SERVICE_URL: http://promotion-service:8091
      REVIEWS_SERVICE_URL: http://reviews-service:8095
      CMS_SERVICE_URL: http://cms-service:8101
      PREFERENCES_SERVICE_URL: http://preferences-service:8094
    depends_on:
      postgres:
        condition: service_healthy
      product-service:
        condition: service_healthy
      cart-service:
        condition: service_healthy
      support-service:
        condition: service_healthy
      store-service:
        condition: service_healthy
      promotion-service:
        condition: service_healthy
      reviews-service:
        condition: service_healthy
      preferences-service:
        condition: service_healthy
      cms-service:
        condition: service_healthy
    networks:
      - vexio-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8099/docs')\" || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 30s

  ia-admin-service:
    build:
      context: ./IaAdmin
    container_name: ia-admin-service
    ports:
      - "127.0.0.1:8102:8102"
    environment:
      DATABASE_URL: postgresql://postgres:${DB_PASSWORD}@postgres:5432/ia_admin
      GEMINI_API_KEY: ${GEMINI_API_KEY}
      GROQ_API_KEY: ${GROQ_API_KEY}
      STORE_SERVICE_URL: http://store-service:8081
      PRODUCT_SERVICE_URL: http://product-service:8084
      REPORTS_SERVICE_URL: http://reports-service:8100
      INVENTORY_SERVICE_URL: http://inventory-service:8089
      SUPPORT_SERVICE_URL: http://support-service:8088
      PROMOTIONS_SERVICE_URL: http://promotion-service:8091
      LOYALTY_SERVICE_URL: http://loyal-customer-service:8093
    depends_on:
      postgres:
        condition: service_healthy
      store-service:
        condition: service_healthy
      product-service:
        condition: service_healthy
      reports-service:
        condition: service_healthy
      inventory-service:
        condition: service_healthy
      support-service:
        condition: service_healthy
      promotion-service:
        condition: service_healthy
      loyal-customer-service:
        condition: service_healthy
    networks:
      - vexio-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8102/docs')\" || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 30s

  payment-service:
    build:
      context: ./Payment
      dockerfile: ../Dockerfile
    container_name: payment-service
    ports:
      - "127.0.0.1:8098:8098"
    environment:
      SERVER_PORT: 8098
      DB_URL: jdbc:postgresql://postgres:5432/payments
      DB_USER: postgres
      DB_PASSWORD: ${DB_PASSWORD}
      MP_ACCESS_TOKEN: ${MP_ACCESS_TOKEN}
      MP_CLIENT_ID: ${MP_CLIENT_ID}
      MP_CLIENT_SECRET: ${MP_CLIENT_SECRET}
      MP_WEBHOOK_SECRET: ${MP_WEBHOOK_SECRET}
      MP_FEE_RATIO: ${MP_FEE_RATIO:-0.01}
      APP_BASE_URL: ${APP_BASE_URL:-http://localhost:8080}
      FRONTEND_URL: ${FRONTEND_URL:-http://localhost:3000}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - vexio-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost 8098 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 90s

  # ── SERVICIOS CON DEPENDENCIAS ENTRE MICROSERVICIOS ──────────────────────────
  # Estos necesitan que otro microservicio esté levantado (no solo la BD).
  # condition: service_started = el contenedor corrió, no espera que el servicio
  # esté 100% listo (Spring Boot tiene reintentos internos si falla al inicio).

  inventory-service:
    build:
      context: ./Inventory
      dockerfile: ../Dockerfile
    container_name: inventory-service
    ports:
      - "127.0.0.1:8089:8089"
    environment:
      SERVER_PORT: 8089
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/inventory
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}
      DB_URL: jdbc:postgresql://postgres:5432/inventory
      DB_USER: postgres
      DB_PASSWORD: ${DB_PASSWORD}
      SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE: 5
      PRODUCT_SERVICE_URL: http://product-service:8084
    depends_on:
      postgres:
        condition: service_healthy
      product-service:
        condition: service_healthy
    networks:
      - vexio-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8089/api/v1/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 90s

  order-payment-service:
    build:
      context: ./OrderPayment
      dockerfile: ../Dockerfile
    container_name: order-payment-service
    ports:
      - "127.0.0.1:8087:8087"
    environment:
      SERVER_PORT: 8087
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/order_payment
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}
      DB_URL: jdbc:postgresql://postgres:5432/order_payment
      DB_USER: postgres
      DB_PASSWORD: ${DB_PASSWORD}
      SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE: 5
      CART_SERVICE_URL: http://cart-service:8086/api/v1
    depends_on:
      postgres:
        condition: service_healthy
      cart-service:
        condition: service_healthy
    networks:
      - vexio-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8087/api/v1/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 90s

  returns-service:
    build:
      context: ./Returns
      dockerfile: ../Dockerfile
    container_name: returns-service
    ports:
      - "127.0.0.1:8092:8092"
    environment:
      SERVER_PORT: 8092
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/returns
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}
      DB_URL: jdbc:postgresql://postgres:5432/returns
      DB_USER: postgres
      DB_PASSWORD: ${DB_PASSWORD}
      SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE: 5
      ORDER_SERVICE_URL: http://order-payment-service:8087/api/v1
    depends_on:
      postgres:
        condition: service_healthy
      order-payment-service:
        condition: service_healthy
    networks:
      - vexio-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8092/api/v1/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 90s

  customer-service:
    build:
      context: .
      dockerfile: Dockerfile.customer
    container_name: customer-service
    ports:
      - "127.0.0.1:8104:8104"
    environment:
      SERVER_PORT: 8104
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/customer
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}
      DB_URL: jdbc:postgresql://postgres:5432/customer
      DB_USER: postgres
      DB_PASSWORD: ${DB_PASSWORD}
      SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE: 5
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - vexio-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8104/api/v1/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 90s

  pos-sale-service:
    build:
      context: .
      dockerfile: Dockerfile.pos-sale
    container_name: pos-sale-service
    ports:
      - "127.0.0.1:8105:8105"
    environment:
      SERVER_PORT: 8105
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/pos_sale
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}
      DB_URL: jdbc:postgresql://postgres:5432/pos_sale
      DB_USER: postgres
      DB_PASSWORD: ${DB_PASSWORD}
      SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE: 5
      INVENTORY_SERVICE_URL: http://inventory-service:8089/api/v1
    depends_on:
      postgres:
        condition: service_healthy
      inventory-service:
        condition: service_healthy
    networks:
      - vexio-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8105/api/v1/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 90s

  transaction-service:
    build:
      context: .
      dockerfile: Dockerfile.transaction
    container_name: transaction-service
    ports:
      - "127.0.0.1:8103:8103"
    environment:
      SERVER_PORT: 8103
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/transaction
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}
      DB_URL: jdbc:postgresql://postgres:5432/transaction
      DB_USER: postgres
      DB_PASSWORD: ${DB_PASSWORD}
      SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE: 5
      MP_ACCESS_TOKEN: ${MP_ACCESS_TOKEN}
      MP_WEBHOOK_SECRET: ${MP_WEBHOOK_SECRET}
      MP_NOTIFICATION_URL: ${MP_NOTIFICATION_URL:-http://localhost:8103/api/v1/transactions/webhook}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - vexio-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8103/api/v1/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 90s

  ia-store-builder-service:
    build:
      context: ./IaStoreBuilder
    container_name: ia-store-builder-service
    ports:
      - "127.0.0.1:8113:8113"
    environment:
      DATABASE_URL: postgresql://postgres:${DB_PASSWORD}@postgres:5432/ia_store_builder
      GROQ_API_KEY: ${GROQ_API_KEY}
      GEMINI_API_KEY: ${GEMINI_API_KEY}
      STORE_SERVICE_URL: http://store-service:8081
    depends_on:
      postgres:
        condition: service_healthy
      store-service:
        condition: service_healthy
    networks:
      - vexio-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8113/docs')\" || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 30s

  # ── GATEWAY (último en levantar) ─────────────────────────────────────────────
  # El Gateway enruta todas las peticiones externas a los microservicios.
  # SPRING_PROFILES_ACTIVE=docker activa application-docker.yml, que reemplaza
  # los "localhost" de application.yaml por los nombres de servicio de Docker.
  gateway:
    build:
      context: ./Gateway
      dockerfile: ../Dockerfile
    container_name: gateway
    ports:
      - "8080:8080"   # Único puerto expuesto al exterior; todo pasa por aquí
    environment:
      SERVER_PORT: 8080
      JWT_SECRET: ${JWT_SECRET}
      SPRING_PROFILES_ACTIVE: docker
    depends_on:
      auth-service:
        condition: service_healthy
      users-service:
        condition: service_healthy
      store-service:
        condition: service_healthy
      product-service:
        condition: service_healthy
      cart-service:
        condition: service_healthy
      order-payment-service:
        condition: service_healthy
      inventory-service:
        condition: service_healthy
      support-service:
        condition: service_healthy
      supplier-service:
        condition: service_healthy
      promotion-service:
        condition: service_healthy
      returns-service:
        condition: service_healthy
      loyal-customer-service:
        condition: service_healthy
      preferences-service:
        condition: service_healthy
      reviews-service:
        condition: service_healthy
      media-service:
        condition: service_healthy
      ia-user-service:
        condition: service_healthy
      ia-admin-service:
        condition: service_healthy
      cms-service:
        condition: service_healthy
      reports-service:
        condition: service_healthy
      customer-service:
        condition: service_healthy
      pos-sale-service:
        condition: service_healthy
      transaction-service:
        condition: service_healthy
      ia-store-builder-service:
        condition: service_healthy
    networks:
      - vexio-net
    restart: unless-stopped
